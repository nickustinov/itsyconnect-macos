services:
  itsyconnect:
    image: ghcr.io/nickustinov/itsyconnect:latest
    # build: .  # uncomment to build locally
    ports:
      - "3000:3000"
    volumes:
      - data:/app/data
    restart: unless-stopped

volumes:
  data:
